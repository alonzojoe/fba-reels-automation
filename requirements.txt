kokoro>=0.9.0
soundfile>=0.13.0
# Pin transformers to 4.x and numpy to 1.x: Kokoro pulls in transformers and
# torch, but transformers v5 needs torch>=2.4, and torch 2.2 (the last wheel
# for Intel macOS) was built against numpy 1.x. Drop these pins on ARM Macs /
# Linux where torch >= 2.4 wheels are available.
transformers<5.0
numpy<2
faster-whisper>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
